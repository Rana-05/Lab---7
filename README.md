# Lab-7

In this lab, I loaded and explored the dataset, visualized relationships between features, and selected relevant variables for modeling. Then, split the data, trained a Logistic Regression model, and evaluated its performance using predictions and a classification report.
